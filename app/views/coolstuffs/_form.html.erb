<%= form_for(@coolstuff) do |f| %>
  <% if @coolstuff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coolstuff.errors.count, "error") %> prohibited this coolstuff from being saved:</h2>

      <ul>
      <% @coolstuff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
